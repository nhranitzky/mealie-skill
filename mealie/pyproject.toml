[project]
name = "mealie"
version = "1.0.0"
description = "CLI for the Mealie self-hosted recipe manager"
requires-python = ">=3.11"
license = { text = "MIT" }

dependencies = [
    "click>=8.1",
    "rich>=13.0",
    "requests>=2.31",
]

[dependency-groups]
dev = ["ruff>=0.4"]

